# Contributing

The full contributor guide is [`docs/DEVELOPER-GUIDE.md`](docs/DEVELOPER-GUIDE.md):
build, test, lint policy, fuzzing, and the acceptance scripts.

Before your first commit, install the hooks once per clone:

```sh
pre-commit install --install-hooks
```
